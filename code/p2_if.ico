global n
procedure main ()
    write("choose a number")
    n:=read()
    if not numeric(n) then write("This is not a number") & return
    if n<0 then write("It is negative")
    else if n%2 = 1 then write ("odd") else write ("even")
end
