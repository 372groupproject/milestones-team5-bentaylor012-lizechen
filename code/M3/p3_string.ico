global x,input

procedure main()
    x:="hello"
    write("input anything below:")
    input :=read()
    write("type("||input||") = "||type(input))
    write("Let x = '"||x||"'")
    write("x || input = " || (x||input))
end